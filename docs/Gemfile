# Local preview only. GitHub Pages builds the site itself with its own
# (older) Jekyll stack, which cannot run on modern Ruby — so we preview
# with Jekyll 4 + the same remote theme instead.
source "https://rubygems.org"
gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme"
gem "jekyll-seo-tag"
gem "jekyll-include-cache"
gem "webrick"
